# --- Common ---
# Production login. Use https://test.salesforce.com for Sandbox.
SF_LOGIN_URL=https://login.salesforce.com

# --- OAuth with Refresh Token (Connected App) ---
SF_CLIENT_ID=
SF_CLIENT_SECRET=
SF_REDIRECT_URI=
SF_REFRESH_TOKEN=
SF_INSTANCE_URL=
SF_ACCESS_TOKEN=

# Optional Username-Password (not used when refresh token vars are set)
# SF_USERNAME=
# SF_PASSWORD=
# SF_SECURITY_TOKEN=

# Optional: override the sample query (must be valid SOQL)
# SF_SOQL=SELECT Id, Name FROM Account ORDER BY CreatedDate DESC LIMIT 5
